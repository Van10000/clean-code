using System;
using Markdown.MarkdownEnumerable;

namespace Markdown
{
    internal static class MarkdownParsingExtentions
    {
        public static string ParseUntil(this IMarkdownEnumerable markdown, Tag[] tags, out Tag stoppedAt)
        {
            throw new NotImplementedException();
        }
    }
}
